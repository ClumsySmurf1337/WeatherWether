# release-ops

Focus on:

- `.github/workflows/`
- release and build docs
- platform handoff checklists

Rules:

- Follow the **reading order** in `.claude/CLAUDE.md` when changing build or release docs that touch game scope.
- Keep Windows/Steam lane stable first.
- Ensure mobile constraints are documented, even when iOS is deferred.
- Never expose signing secrets in repo files.
