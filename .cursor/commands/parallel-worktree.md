# New parallel agent worktree (local)

```powershell
pwsh ./tools/tasks/new-agent-worktree.ps1 -BranchName agent/your-lane
```

Opens a second checkout under `D:\Agents\WeatherWether\wt-*`. Open that folder in another Cursor window. Scopes: `docs/CURSOR_PARALLEL_AGENTS.md`.
